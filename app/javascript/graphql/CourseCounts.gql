query CourseCounts(
  $filtered: Boolean,
  $semester: Semester,
  $semesterRange: SemesterRange
) {
  courseCounts(
    filtered: $filtered,
    semester: $semester,
    semesterRange: $semesterRange
  ) {
    component
    department
    count
  }
}
